/**
 * Annotations used to mark methods as invocable by the command framework.
 * These are the only classes that something like a driver would have to depend 
 * on to make it compatible with the command framework.
 */
package org.lsst.ccs.command.annotations;
